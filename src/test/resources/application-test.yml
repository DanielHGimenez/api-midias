spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  cloud:
    gcp:
      credentials:
        location: # colocar o endereço do arquivo JSON com as credenciais do GCP

gcp:
  storage:
    bucket:
      nome: # preencha com o nome do bucket de teste da GCP storage

